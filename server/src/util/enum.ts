export enum TagType {
  Lookup = "Lookup Routes",
  Product = "Product Routes"
}

export enum NavType {
  Health = "/health",
  Swagger = "/swagger",
  Public = "/pub",
  Categories = "/categories",
  Product = "/product",
  Products = "/products"
}
